#pragma once

#include <string.h>
#include <algorithm>
#include <memory>
#include <functional>
#include <bitset>

using namespace std;
string encode(string toEncodeS);
string decode(string toDecodeS);
